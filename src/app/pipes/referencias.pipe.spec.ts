import { ReferenciasPipe } from './referencias.pipe';

describe('ReferenciasPipe', () => {
  it('create an instance', () => {
    const pipe = new ReferenciasPipe();
    expect(pipe).toBeTruthy();
  });
});
